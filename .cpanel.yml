deployment:
  tasks:
    - export DEPLOYPATH=/home/kanpower/public_html
    - export REPOPATH=/home/kanpower/repositories/server/convert_PDF_to_QRCode
    # Delete the .git directory to avoid conflicts with deployment.
    - /bin/rm -rf $REPOPATH/.git
    # Delete the contents of the public_html directory.
    - /bin/rm -rf $DEPLOYPATH/*

